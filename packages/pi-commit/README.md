# @testzugang/pi-commit

Interactive gitmoji-based commit skill with staged review for Pi. It can stage recent session changes after one explicit file-selection confirmation.

## Install

```bash
pi install npm:@testzugang/pi-commit
```

## Usage

```text
/skill:commit
```
